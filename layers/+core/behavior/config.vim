" Shorten the time before the vim-leader-guide buffer appears
set timeoutlen=300
set ttimeoutlen=0

" Read modelines from files
set modeline

" Display menu keys with a "+" in-front of the description if they expand
" into a submenu.
let g:leaderGuide_display_plus_menus = 1
